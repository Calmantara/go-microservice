service:
  name: go-gateway
  httpPort: 8080
goWallet:
  host: "localhost:50050"
  # in second
  timeout: 10
  timeToWait: 10
  maxRetries: 3
goEmitter:
  host: "localhost:50051"
  # in second
  timeout: 10
  timeToWait: 10
  maxRetries: 3